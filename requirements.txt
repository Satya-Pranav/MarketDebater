# Core
python-dotenv>=1.0.0

# Market data — yfinance for US EOD OHLCV; technicals hand-rolled in pandas to
# avoid the pandas-ta / numpy-2 incompatibility.
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26

# News fallback — used only when GROK_API_KEY is unset (Grok web search is primary).
feedparser>=6.0

# LLM brain — works for Azure OpenAI / Foundry, standard OpenAI, AND xAI Grok.
openai>=1.40

# RAG (Azure AI Search)
azure-search-documents>=11.5.0

# Verdict persistence (Azure Blob Storage)
azure-storage-blob>=12.20.0

# UI
streamlit>=1.36

# Django frontend (keep for the Django UI)
Django>=5.0
# Production WSGI server (Azure App Service runs this)
gunicorn>=22.0
# Static-file middleware so Django serves its own static assets in prod
# without needing nginx / Azure Storage.
whitenoise>=6.7

# Tests
pytest>=8.0

# NOTE: Microsoft Agent Framework SDK lives in requirements-maf.txt to keep the
# base install small and Python-3.9-compatible.
